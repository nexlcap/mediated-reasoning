# LiteLLM routes to any provider — set the key for the provider(s) you use.
# Default model is Anthropic Claude Sonnet. Add other keys if switching providers.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: OpenAI models (--model gpt-4o, --model gpt-4o-mini, etc.)
# OPENAI_API_KEY=your_openai_api_key_here

# Optional: Together AI for open-source models hosted in the cloud
# TOGETHER_AI_API_KEY=your_together_ai_api_key_here

# Local models via Ollama — no API key needed, just run `ollama serve`
# Use: --model ollama/llama3.3  or  --model ollama/phi4

# Optional: Tavily for higher-quality search (pip install -r requirements-tavily.txt)
# Without this, DuckDuckGo is used automatically — no account or API key required.
# TAVILY_API_KEY=your_tavily_api_key_here

# Optional: Langfuse observability (https://langfuse.com)
# pip install -r requirements-langfuse.txt
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...

name: Sync to HuggingFace Space

on:
  push:
    branches: [main]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Push to HuggingFace Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config --global user.email "ci@github"
          git config --global user.name "GitHub CI"

          # Clone the HF Space repo
          git clone https://Yi47:${HF_TOKEN}@huggingface.co/spaces/Yi47/mediated-reasoning hf_space

          # Sync app.py
          cp app.py hf_space/app.py

          # Pin requirements.txt to current commit SHA to force package reinstall
          sed "s/COMMIT_SHA/${GITHUB_SHA}/" requirements-hf.txt > hf_space/requirements.txt

          cd hf_space
          git add app.py requirements.txt
          if git diff --cached --quiet; then
            echo "No changes to sync."
          else
            git commit -m "Sync from GitHub (${GITHUB_SHA::7})"
            git push
          fi
